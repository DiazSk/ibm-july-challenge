# Scraping
instaloader

# Text cleaning
ftfy

# LangChain core + Ollama integration
langchain>=0.2.0
langchain-core>=0.2.0
langchain-ollama>=0.2.0

# Local embeddings + clustering
sentence-transformers>=3.0.0
scikit-learn>=1.4.0

# Vector store (Week 2)
chromadb>=0.5.0

# Data wrangling
pandas>=2.0.0
numpy>=1.24.0

# Config
python-dotenv>=1.0.0

# Frontend (Streamlit — kept as fallback)
streamlit>=1.35.0

# API backend
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# JARVIS agent — web search for creator inspiration (no API key needed)
# ddgs — OPTIONAL. Only JARVIS's "research trending content" tool uses it;
# every other feature runs entirely on local data. Install it if you want
# that tool, otherwise JARVIS says web research is unavailable.
ddgs>=9.0.0


# Vision preprocessor — Reel keyframe extraction (imageio-ffmpeg bundles the
# ffmpeg binary, so no system install is needed on Windows). ollama + Pillow
# come in transitively via langchain-ollama / sentence-transformers.
imageio>=2.34.0
imageio-ffmpeg>=0.5.0

# Voice pipeline — Whisper STT + Kokoro TTS (Voicebox's built-in engine)
# Windows/CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu121 first
faster-whisper>=1.0.0
kokoro>=0.9.2
soundfile>=0.12.1
